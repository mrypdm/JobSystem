@model Guid[]

@{
    ViewData["Title"] = "Jobs";
}

<table>
    <tr>
        <th>ID</th>
        <th></th>
    </tr>

@foreach (var jobId in Model)
{
    <tr>
        <td>@(jobId)</td>
        <td>
            <input
                style="width: auto"
                type="button"
                value="View Results"
                onclick="location.replace('/jobs/@(jobId)')" />
        </td>
    </tr>
}

</table>
